package com.red.alert.config;

public class Notifications
{
    // The disconnected notification ID
    public static final int CONNECTIVITY_NOTIFICATION_ID = 1024;

    // The time (in millis) until the disconnected notification will be hidden when connection is re-established
    public static final int CONNECTIVITY_NOTIFICATION_HIDE_TIMEOUT = 5 * 1000;
}
